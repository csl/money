<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
      package="com.money"
      android:versionCode="1"
      android:versionName="1.0">
    <application android:icon="@drawable/icon" android:label="@string/app_name">
        <activity android:name=".MoneyListView"
                  android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
     <activity android:name="additem"></activity>>
     <activity android:name="query"></activity>>
     <activity android:name="invoice"></activity>>        
     <activity android:name="addaccount"></activity>>        
     <activity android:name="calc"></activity>>        
     <activity android:name="addgunno"></activity>>        
    </application>
</manifest> 
